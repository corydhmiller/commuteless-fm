name: Scheduled build
on:
  schedule:
  - cron: '0 4 * * 2'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Trigger our build webhook on Netlify
      run: curl -s -X POST "${TOKEN}"
      env:
        TOKEN: ${{ secrets.DAILY_NETLIFY_BUILD_URL }}